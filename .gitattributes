# Shell scripts and server-side files must stay LF so they run on Linux,
# even though the repo is developed on Windows.
*.sh text eol=lf
deploy/* text eol=lf
*.service text eol=lf
*.conf text eol=lf
