export default function SitesPage() {
  return (
    <>
      <h1>Sites</h1>
      <p className="placeholder">
        Sites list (domain, client, server, install status, SSL expiry,
        Lighthouse), the new-site wizard, and the post-install record land in
        Milestones M2–M4.
      </p>
    </>
  );
}
